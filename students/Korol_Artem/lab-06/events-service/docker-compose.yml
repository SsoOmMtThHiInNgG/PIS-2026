version: '3.8'

services:

  db:
    image: postgres:15

    container_name: events-db

    environment:
      POSTGRES_DB: events
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"