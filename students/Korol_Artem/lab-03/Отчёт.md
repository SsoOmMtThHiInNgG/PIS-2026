<p align="center">Министерство образования Республики Беларусь</p>
<p align="center">Учреждение образования</p>
<p align="center">"Брестский Государственный технический университет"</p>
<p align="center">Кафедра ИИТ</p>

<br><br><br><br><br><br>

<p align="center"><strong>Лабораторная работа №3</strong></p>
<p align="center"><strong>По дисциплине:</strong> "Проектирование интернет-систем"</p>
<p align="center"><strong>Тема:</strong> "Реализация Domain Layer с DDD-паттернами"</p>

<br><br><br><br><br><br>

<p align="right"><strong>Выполнил:</strong></p>
<p align="right">Студент 3 курса</p>
<p align="right">Группа ПО-13</p>
<p align="right">Король А.В.</p>

<p align="right"><strong>Проверил:</strong></p>
<p align="right">Шорох Д.В.</p>

<br><br><br><br><br>

<p align="center"><strong>Брест 2026</strong></p>

---

## Цель работы

Научиться применять тактические паттерны DDD (Entities, Value Objects, Aggregates, Domain Events) для реализации доменного слоя с инвариантами и доменной логикой.

---

## Вариант №15 - Ивенты «Зафичь, идём?» 🎤

**Питч:** Мероприятие без бардака.

**Ядро домена:** События, Билеты, Участники, Расписание, Спикеры

---

## Ход выполнения работы

### 1. Value Objects (Ценностные объекты)

### Созданные Value Objects:

---

### 1. EventTitle – название мероприятия

- Назначение: хранит название события
- Валидация:
  - строка не пустая
  - длина ≥ 3 символов
- Иммутабельность: ✅
- Файл: `domain/value_objects/EventTitle.java`

---

### 2. TicketPrice – стоимость билета

- Назначение: хранит цену билета
- Валидация:
  - цена > 0
  - значение не null
- Иммутабельность: ✅
- Файл: `domain/value_objects/TicketPrice.java`

---

### 3. SpeakerName – имя спикера

- Назначение: хранит имя выступающего
- Валидация:
  - строка не пустая
  - длина ≥ 2 символов
- Иммутабельность: ✅
- Файл: `domain/value_objects/SpeakerName.java`

---

### 4. EventTimeSlot – временной интервал события

- Назначение: время начала и окончания мероприятия
- Валидация:
  - start < end
  - значения не null
- Иммутабельность: ✅
- Файл: `domain/value_objects/EventTimeSlot.java`

---

### 5. Location – место проведения мероприятия

- Назначение: адрес проведения события
- Валидация:
  - строка не пустая
  - значение не null
- Иммутабельность: ✅
- Файл: `domain/value_objects/Location.java`

---

## Пример Value Object

```java
public record EventTimeSlot(
    LocalDateTime start,
    LocalDateTime end
) {

    public EventTimeSlot {

        if (start == null || end == null)
            throw new IllegalArgumentException(
                "Start and end cannot be null"
            );

        if (!start.isBefore(end))
            throw new IllegalArgumentException(
                "Start must be before end"
            );
    }

    public boolean overlaps(EventTimeSlot other) {
        return start.isBefore(other.end())
            && other.start().isBefore(end);
    }
}
```

---

## 2. Entities (Сущности)

### Созданные Entity:

---

### 1. Event – мероприятие

- ID поле: `id`
- Бизнес-правила:
  - нельзя публиковать уже опубликованное событие
  - нельзя изменить завершённое мероприятие
  - название не может быть пустым
  - мероприятие должно иметь расписание
- Файл: `domain/entities/Event.java`

---

### 2. Ticket – билет

- ID поле: `id`
- Бизнес-правила:
  - билет нельзя активировать повторно
  - билет обязательно привязан к событию
  - цена билета должна быть положительной
- Файл: `domain/entities/Ticket.java`

---

### 3. Speaker – спикер

- ID поле: `id`
- Бизнес-правила:
  - имя спикера обязательно
  - спикер не может быть назначен на два события одновременно
- Файл: `domain/entities/Speaker.java`

---

### 4. Schedule – расписание

- ID поле: `id`
- Бизнес-правила:
  - нельзя добавлять пересекающиеся временные интервалы
  - расписание нельзя изменять после блокировки
  - временные интервалы должны быть валидными
- Файл: `domain/entities/Schedule.java`

---

## Пример Entity

```java
public class Event {

    private final Long id;
    private EventTitle title;
    private Location location;
    private boolean published = false;

    private final List<Object> events = new ArrayList<>();

    public Event(
        Long id,
        EventTitle title,
        Location location
    ) {

        if (id == null)
            throw new IllegalArgumentException(
                "Event ID cannot be null"
            );

        this.id = id;
        this.title = title;
        this.location = location;
    }

    public void publish() {

        if (published)
            throw new IllegalStateException(
                "Event already published"
            );

        this.published = true;

        events.add(
            new EventPublishedEvent(id)
        );
    }

    public void rename(EventTitle newTitle) {

        if (published)
            throw new IllegalStateException(
                "Cannot rename published event"
            );

        this.title = newTitle;
    }

    public boolean isPublished() {
        return published;
    }
}
```

---

## 3. Aggregate Root (Корневой агрегат)

### Aggregate Root: Ticket

### Границы агрегата:

- Корень: `Ticket`
- Внутренние сущности: отсутствуют
- Value Objects:
  - `TicketPrice`
  - `EventTimeSlot`

---

## Инварианты агрегата

| № | Инвариант | Как проверяется |
|---|---|---|
| 1 | ID и eventId не могут быть null | Проверка в конструкторе |
| 2 | Цена билета должна быть положительной | Валидация TicketPrice |
| 3 | При создании билет всегда имеет статус CREATED | Поле status |
| 4 | При создании регистрируется TicketCreatedEvent | В конструкторе |
| 5 | Нельзя активировать уже активированный билет | Проверка в activate() |
| 6 | Нельзя отменить использованный билет | Проверка в cancel() |
| 7 | Каждое изменение состояния создаёт доменное событие | events.add(...) |

---

## Пример Aggregate Root

```java
public class Ticket {

    private final Long id;
    private final Long eventId;

    private TicketStatus status =
        TicketStatus.CREATED;

    private final List<DomainEvent> events =
        new ArrayList<>();

    public Ticket(
        Long id,
        Long eventId
    ) {

        if (id == null)
            throw new IllegalArgumentException(
                "Ticket ID cannot be null"
            );

        if (eventId == null)
            throw new IllegalArgumentException(
                "Event ID cannot be null"
            );

        this.id = id;
        this.eventId = eventId;

        events.add(
            new TicketCreatedEvent(id, eventId)
        );
    }

    public void activate() {

        if (status == TicketStatus.ACTIVE)
            throw new IllegalStateException(
                "Ticket already active"
            );

        status = TicketStatus.ACTIVE;

        events.add(
            new TicketActivatedEvent(id)
        );
    }

    public void cancel() {

        if (status == TicketStatus.USED)
            throw new IllegalStateException(
                "Cannot cancel used ticket"
            );

        status = TicketStatus.CANCELLED;

        events.add(
            new TicketCancelledEvent(id)
        );
    }

    public List<DomainEvent> getEvents() {
        return List.copyOf(events);
    }
}
```

---

## 4. Domain Events (Доменные события)

### Созданные события:

---

### 1. TicketCreatedEvent

- Генерируется при создании билета
- Данные:
  - ticket_id
  - event_id
  - occurred_on
- Файл:
  `domain/events/TicketCreatedEvent.java`

---

### 2. TicketActivatedEvent

- Генерируется при активации билета
- Данные:
  - ticket_id
  - occurred_on
- Файл:
  `domain/events/TicketActivatedEvent.java`

---

### 3. TicketCancelledEvent

- Генерируется при отмене билета
- Данные:
  - ticket_id
  - occurred_on
- Файл:
  `domain/events/TicketCancelledEvent.java`

---

### 4. EventPublishedEvent

- Генерируется при публикации мероприятия
- Данные:
  - event_id
  - occurred_on
- Файл:
  `domain/events/EventPublishedEvent.java`

---

## Пример Domain Event

```java
public class TicketCreatedEvent
    implements DomainEvent {

    private final Long ticketId;
    private final Long eventId;
    private final Instant occurredOn;

    public TicketCreatedEvent(
        Long ticketId,
        Long eventId
    ) {

        this.ticketId = ticketId;
        this.eventId = eventId;
        this.occurredOn = Instant.now();
    }

    @Override
    public Instant occurredOn() {
        return occurredOn;
    }
}
```

---

## 5. Юнит-тесты

### Покрытие тестами

| Компонент | Количество тестов | Покрытие | Статус |
|-----------|-------------------|----------|--------|
| Value Objects | 10 | 100% | ✅ |
| Entities | 14 | 100% | ✅ |
| Aggregate Root | 7 | 100% | ✅ |
| Domain Events | 4 | 100% | ✅ |

---

## Скриншот Maven test

![Тесты](./test.jpg)

---

## Таблица критериев оценки

| Критерий | Баллы | Выполнено |
|----------|-------|-----------|
| Value Objects | 20 | ✅ |
| Entities | 20 | ✅ |
| Aggregate Root | 25 | ✅ |
| Domain Events | 15 | ✅ |
| Юнит-тесты | 15 | ✅ |
| Документация | 5 | ✅ |
| **ИТОГО** | **100** | |

---

## Контрольные вопросы

1. **В чём отличие Value Object от Entity?**
    - **Value Object** определяется *значением* и не имеет собственного идентификатора.  
    - **Entity** определяется *личностью* (ID) и сохраняет свою идентичность даже при изменении состояния.

2. **Почему Aggregate Root должен инкапсулировать доступ к внутренним сущностям?**
    - Чтобы гарантировать соблюдение **инвариантов агрегата** и не позволить внешнему коду изменять состояние напрямую.Все изменения проходят через методы Aggregate Root, что обеспечивает целостность данных и корректные переходы состояний.

3. **Какая роль Domain Events? Приведите пример из вашей системы.**
   - Domain Events фиксируют **значимые изменения в доменной модели**, чтобы другие части системы могли реагировать на них, не нарушая изоляцию домена.

4. **Как вы проверяете инварианты в вашем агрегате? Приведите пример.**
   - Инварианты проверяются в **конструкторе** и **методах изменения состояния**.
    Пример:  
    В методе `complete()` проверяется, что текущее время позже `slot.end()`, иначе бросается исключение: "Cannot complete booking before it ends"

5. **Почему Value Objects делаются иммутабельными?**
   - Потому что они представляют **значения**, а не сущности. Иммутабельность гарантирует, что значение не изменится неожиданно, что делает модель предсказуемой, безопасной и удобной для использования в инвариантах и сравнениях.

---

## Структура проекта

```text
lab-03/
├── test.jpg
├── README.md
├── Отчет.md
├── domain/
│   ├── entities/
│   │   ├── Event.java
│   │   ├── Ticket.java
│   │   ├── Speaker.java
│   │   └── Schedule.java
│   │
│   ├── events/
│   │   ├── DomainEvent.java
│   │   ├── TicketCreatedEvent.java
│   │   ├── TicketActivatedEvent.java
│   │   ├── TicketCancelledEvent.java
│   │   └── EventPublishedEvent.java
│   │
│   └── value_objects/
│       ├── EventTitle.java
│       ├── TicketPrice.java
│       ├── SpeakerName.java
│       ├── EventTimeSlot.java
│       └── Location.java
│
└── tests/
    ├── TicketTest.java
    ├── EventTest.java
    ├── SpeakerTest.java
    └── ScheduleTest.java
```

---

## Вывод

В ходе лабораторной работы был реализован доменный слой системы мероприятий «Зафичь, идём?» 🎤 с использованием DDD-паттернов. Были созданы сущности, value objects, aggregate root и доменные события. Все инварианты агрегатов контролируются внутри доменной модели, а бизнес-логика изолирована от инфраструктурного слоя.

---

**Дата выполнения:** 20.04.2026

**Оценка:** _____________

**Подпись преподавателя:** _____________